"""interface is nothing but abstract class which contains only abstract method but not any normal method"""
"""syntax:
import abc
class interface_class(ABC)
    @abstractmethod
    def fun(self)
        pass
    
    @abstractmethod
    def fun2(self):
        pass"""

"""
1)we cantcreate object of interface
2)we use interface when a action is common but implementations
3)all child class should inheritabstract methods
"""